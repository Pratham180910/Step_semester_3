## Date: 11-09-2026

**Today's Work:**
Completed Session 5. Created branch `feature/session_5` from `develop` and finished class and assignment problems for OOP Fundamentals.

**Next Session Plan:**
Create the `feature/session_6` branch from `develop` and work on the next topic.

**Issues Faced:**
- VS Code locked the .vscode and src directories during checkout. Bypassed the prompts and manually deleted the folders on the main branch.

## Date: 04-09-2026

**Today's Work:**
Completed Session 4. Created branch `feature/session_4` from `develop` and finished class and assignment problems for Arrays.

**Next Session Plan:**
Create the `feature/session_5` branch from `develop` and work on the next topic.

**Issues Faced:**
- VS Code locked the directory during checkout. Bypassed the prompt and manually deleted the src folder on the main branch.

## Date: 31-08-2026

**Today's Work:**
Completed Session 3. Created the `feature/session_3` branch from `develop` and finished the class and take-home assignment problems for this session.

**Next Session Plan:**
Create the `feature/session_4` branch from `develop` and work on the next topic.

**Issues Faced: **
- None

## Date: 31-08-2026

**Today's Work:**
Completed Session 2. Created branch `feature/session_2` from `develop` and finished class and assignment problems for String Operations.

**Next Session Plan:**
Create `feature/session_3` from the `develop` branch and work on the next topic.

**Issues Faced:**
- VS Code Java extension locked the folder during branch switch. Manually deleted the src folder on the main branch to keep it clean.

## Date: 31-08-2026

**Today's Work:**
Set up the GitHub repository strictly following the Step Semester 3 branching model. Initialized the `main` and `develop` branches. Created `feature/session_1` and completed both class and assignment problems for the String topic.

**Next Session Plan:**
Create the `feature/session_2` branch from `develop` and complete the coding problems for the next topic. 

**Issues Faced: **
- Git failed to delete the `src` directory when switching branches because the Java files were still open in the VS Code editor. Resolved by closing the editor tabs and manually deleting the leftover folder to keep the `main` branch clean.

